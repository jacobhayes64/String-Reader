# String Reader
A program built with C++ which uses statically allocated memory to take input(s) and save it to a dyamically memory allocated vector. The program can print the full vector and offers options to delete by reference, read by reference, and clear the whole vector.
